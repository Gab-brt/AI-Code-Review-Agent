# AI-Code-Review-Agent
The AI Code Review Agent is a Python command-line tool that helps students review Python source files. It reads a selected file, analyzes its structure, detects basic code quality issues, checks simple metrics, and generates improvement suggestions using an agent-based workflow.
